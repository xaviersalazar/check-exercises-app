{
  "name": "check-exercises",
  "version": "1.3.5",
  "description": "check your missing codeup exercises",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-mac": "electron-packager . --ignore=test-builds --overwrite --platform=darwin --arch=x64 --prune=true --out=release-builds",
    "package-mac-test": "electron-packager . --ignore=release-builds --overwrite --platform=darwin --arch=x64 --prune=true --out=test-builds"
  },
  "author": "Xavier Salazar",
  "license": "MIT",
  "devDependencies": {
    "electron": "^2.0.4"
  },
  "dependencies": {
    "bootstrap": "^4.1.2",
    "electron-packager": "^12.1.0",
    "jquery": "^3.3.1",
    "popper.js": "^1.14.3",
    "scrollmagic": "^2.0.5",
    "typed.js": "^2.0.8",
    "web-animations-js": "^2.3.1"
  }
}
